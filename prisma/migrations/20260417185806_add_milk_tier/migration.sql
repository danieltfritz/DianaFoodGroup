BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[School] ADD [milkTier] VARCHAR(10) NOT NULL CONSTRAINT [School_milkTier_df] DEFAULT 'medium';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
